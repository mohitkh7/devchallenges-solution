import { FilterTopPipe } from './filter-top.pipe';

describe('FilterTopPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTopPipe();
    expect(pipe).toBeTruthy();
  });
});
