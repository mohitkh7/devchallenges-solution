import { DisplayGuestCountPipe } from './display-guest-count.pipe';

describe('DisplayGuestCountPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayGuestCountPipe();
    expect(pipe).toBeTruthy();
  });
});
